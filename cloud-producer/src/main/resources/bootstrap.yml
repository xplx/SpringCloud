eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9000/eureka/
server:
  port: 9002
spring:
  application:
    name: cloud-producer



logging:
  file: "logs/eureka-client-one.log"

endpoints:
  health:
    sensitive: false
    enabled: true
  actuator:
    enabled: true
    sensitive: false
  beans:
    sensitive: false
    enabled: true


management:
  security:
    enabled: false